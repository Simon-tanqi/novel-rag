# ============================================================
# NovelRAG 环境变量模板
# 使用方法：
#   1. 复制本文件为 .env：   cp .env.example .env   (Windows: copy .env.example .env)
#   2. 填入你的 DeepSeek API Key（https://platform.deepseek.com 获取）
#   3. 运行任意命令（python novel_rag.py / python main.py），项目会自动加载 .env
#
# 说明：
#   - .env 已被 .gitignore 排除，不会提交到 GitHub，密钥安全；
#   - 真实环境变量优先级更高（已设置的键不会被 .env 覆盖）；
#   - 也可完全不使用 .env，直接用系统环境变量（推荐，密钥更安全）。
# ============================================================

# DeepSeek API Key（必填，否则 ask/chat 无法调用模型）
DEEPSEEK_API_KEY=

# 嵌入模型（可选）：默认 BAAI/bge-small-zh-v1.5（首次运行自动下载）。
# 可改为本地模型目录路径；留空/删除该行 = 使用默认。
# EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5
